// Firebase initializer removed during revert.
// If you want Firebase back, re-add a proper firebase.ts with your Vite env vars.
export const USE_FIREBASE = false;
export const app = null;
export const auth = null;
export const db = null;
